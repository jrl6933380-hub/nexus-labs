# AI Gateway text-generation test (throwaway verification script)

Not part of the app — a standalone script to mechanically verify the AI SDK
streamText + AI Gateway pattern works, separate from Nex's own delegate_to_model.
Lives on this branch only; not intended to be merged to main.
