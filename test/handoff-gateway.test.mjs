import test from 'node:test';import assert from 'node:assert/strict';import { buildHandoffPacket } from '../lib/handoffGateway.js';test('handoff caps file context',()=>{const p=buildHandoffPacket({goal:'Add room button',files:[{path:'public/index.html',content:'x'.repeat(5000)}]});assert.equal(p.files[0].path,'public/index.html');assert.equal(p.files[0].truncated,true);assert.ok(p.files[0].excerpt.length<4100)});